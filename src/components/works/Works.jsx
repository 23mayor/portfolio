import styled from "styled-components";
import { List } from "./List";

const Wrapper = styled.section`

`;

export const Works = () => {
    return (
        <Wrapper>
            <List />
        </Wrapper>
    )
}